kind = "web"
previewPath = "/"
title = "DevOtion"
version = "1.0.0"
id = "artifacts/devotion"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20803

[services.development]
run = "pnpm --filter @workspace/devotion run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/devotion", "run", "build" ]
publicDir = "artifacts/devotion/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20803"
BASE_PATH = "/"
